---
제목: 플랫폼 통합 주소 체계 규격 (v2.0)
날짜: 2026-03-14
작성자: Gemini CLI Agent (with User)
카테고리: 01_표준정의
태그: #URL-Architecture #DX #Convention-over-Configuration
상태: Active
---

# 🗺️ 플랫폼 통합 주소 체계 규격 (v2.0)

> 본 문서는 개발자 경험(DX)을 최우선으로 하여, URL 구조와 실제 파일 경로를 1:1로 매핑하는 **단일 책임 주소 체계**를 정의한다. 모든 URL은 개발자가 요청을 처리하는 파일의 위치를 즉시 파악할 수 있도록 설계되었다.

---

## 1. 주소 설계 원칙 (Core Principles)

1.  **개발자 경험 최우선 (Developer-Experience First)**: URL의 주 소비자는 개발자이다. URL의 구조는 사용자 편의나 SEO보다 코드의 명확성, 예측 가능성, 유지보수성에 기여해야 한다.
2.  **버전 우선 (Version-First)**: 모든 주소는 API 및 시스템 버전을 나타내는 `/v1`을 최상위 경로로 가진다.
3.  **레이어 기반 구조 (Layer-Driven Architecture)**: 모든 주소는 기능의 성격에 따라 `app`, `pages`, `admin`의 3가지 레이어 중 하나에 명시적으로 속한다.
4.  **경로 = 디렉토리 (Path-as-Directory)**: URL 경로는 SvelteKit의 `src/routes` 디렉토리 구조와 1:1로 매핑되어야 한다.

---

## 2. 주소 구조 상세 (Route Anatomy)

### 2.1 일반 구조
모든 URL은 `/{version}/{layer}/{instance}/{record}/{action}`의 계층 구조를 따른다. 뒤따르는 요소는 상황에 따라 생략될 수 있다.

### 2.2 레이어 정의 (Layer Definitions)

| 레이어 | 경로 구조 | 설명 | 관리 페이지 |
| :--- | :--- | :--- |:--- |
| **`app`** | `/v1/app/...` | '앱 구동기' 기반의 동적 기능. DB CRUD 등 복잡한 로직을 포함한다. | **필수** |
| **`pages`** | `/v1/pages/...`| 순수 콘텐츠 페이지. 정적이며, 별도의 설정 기능이 존재하지 않는다. | **없음** |
| **`admin`** | `/v1/admin/...`| **오직 `app` 레이어의 기능**을 설정하기 위한 전용 관리 레이어. | 해당 없음 |

### 2.3 경로 규칙 및 예시

#### `app` & `admin` 레이어 (1:1 대응)
- `admin`은 `app`의 설정을 위한 공간이며, 경로는 `app`의 경로를 그대로 따른다.

| 목적 | 요청 | 설명 |
| :--- | :--- | :--- |
| **앱 설정** | `GET /v1/admin/app/board` | `board` 앱의 전반적인 설정 화면 |
| **인스턴스 설정** | `GET /v1/admin/app/board/notice` | `board` 앱의 `notice` 인스턴스 설정 화면 |
| **레코드 목록** | `GET /v1/app/board/notice` | `notice` 인스턴스의 레코드(게시글) 목록 보기 |
| **개별 레코드** | `GET /v1/app/board/notice/123` | `notice` 인스턴스의 123번 레코드(게시글) 보기 |
| **레코드 액션** | `GET /v1/app/board/notice/123/edit`| 123번 레코드 수정 화면 |

#### `pages` 레이어 (독립 콘텐츠)

| 목적 | 요청 | 설명 |
| :--- | :--- | :--- |
| **페이지 보기** | `GET /v1/pages/about-us` | `about-us` 정적 페이지 보기 (설정 기능 없음) |

---

## 3. 디렉토리 구조 매핑

URL의 레이어는 `svelte/src/routes/v1/` 하위의 디렉토리와 직접 연결된다.

- `/v1/app/*` 요청은 `svelte/src/routes/v1/app/` 내부의 파일이 처리한다.
- `/v1/pages/*` 요청은 `svelte/src/routes/v1/pages/` 내부의 파일이 처리한다.
- `/v1/admin/app/*` 요청은 `svelte/src/routes/v1/admin/app/` 내부의 파일이 처리한다.

---
### 📜 변경 이력
- **2026-03-14**: [v2.0] 개발자 경험 최적화를 위해 경로-디렉토리 1:1 매핑 및 레이어 기반 규칙 확립.
- **2026-03-12**: [v1.0] 이원화 주소 체계 표준 확립.
